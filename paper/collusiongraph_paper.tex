% =====================================================================
%  One Stack, Two Ledgers
%  CollusionGraph -- research paper, written from docs/paper_blueprint.md
%
%  COMPILING ON OVERLEAF
%  ---------------------
%  Upload this .tex together with the seven PNG figures that live in the
%  repository root:
%      fig1_architecture.png        fig5_label_noise.png
%      fig2_two_ledgers.png         fig6_label_efficiency.png
%      fig3_timestep_crater.png     fig7_dual_actor.png
%      fig4_transfer_lift.png
%  Everything else is self-contained -- the tables are inlined, so there
%  is nothing to \input and no external .bib to resolve.
%
%  HOUSE RULE: every number in this paper is traced to an artifact under
%  eval_outputs/ via docs/reproducibility.md. Regenerate the tables with
%  `uv run poe paper-tables` before editing any figure quoted here.
% =====================================================================
\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{xcolor}
\usepackage{balance}
\usepackage[hidelinks]{hyperref}
\usepackage{url}

\newcommand{\RQ}[1]{\textbf{RQ#1}}

\begin{document}

\title{One Stack, Two Ledgers:\\
Budget-Evaluated, Explainable Collusion Screening\\
Across Illicit-Finance and Procurement Networks}

% All five authors share one affiliation, so IEEE style puts them in a single
% centred block with the institution stated once. The e-mail addresses are
% compressed on the shared @spjain.org domain, which is the conventional way to
% keep a five-author list on two lines.
\author{%
\IEEEauthorblockN{Kartik Joshi, Prem Kukreja, Gagandeep Singh,
Samuel Alex, and Rachit Garg}
\IEEEauthorblockA{\textit{SP Jain School of Global Management}\\
Dubai, United Arab Emirates\\
\texttt{\{kartik.as25dxb025,\,prem.as25dxb029,\,gagandeep.as25dxb032,}\\
\texttt{samuel.as25dxb031,\,rachit.garg\}@spjain.org}}
}

\maketitle

\begin{abstract}
Money laundering and bid rigging are studied by separate communities, yet the
two crimes share a shape. Neither can be committed alone, no single
transaction or tender looks wrong by itself, and what gives the scheme away is
the pattern of dealings around it. We built one graph-learning stack for both
domains: a single intermediate representation feeding one detector family, one
explanation layer and one evaluation protocol, with six adapters carrying five
public datasets into it. Our headline result is unfavourable to deep learning.
Under strict inductive splits and fixed alert budgets, gradient-boosted trees
on graph features beat every graph neural network we trained on Elliptic++,
scoring $0.8104$ AUC-PR against $0.4729 \pm 0.0525$ for GATv2-focal across five
seeds. The paired-bootstrap gap is $-0.261$ (95\% CI $[-0.285, -0.235]$,
$p < 0.001$), and running five seeds widens it instead of closing it. Transfer
between procurement markets works, but not everywhere. Held-out markets in the
Garc\'ia data all beat their own base rate, for a macro lift of $1.57$; the
Mendeley EU cartel folds average $1.17$ and yet the largest market of the seven
lands at $0.90$, below the rate you would get by guessing. Across domains the
picture is lopsided. A frozen procurement encoder lifts financial screening
$2.31\times$ over prevalence, the reverse direction falls below prevalence
altogether, and in neither direction does transfer pay for itself at fewer than
about a thousand target labels. One negative result deserves its own sentence:
flipping 20\% of our training labels \emph{raised} test AUC-PR from $0.483$ to
$0.598$ while validation fell from $0.942$ to $0.569$, which makes the blind
spot in shift-era model selection hard to argue with. We also ship the working
system, since a screening result nobody can act on is not much of a result:
PGExplainer subgraphs matched to motifs and cited against FATF and OECD red
flags, a console that reviews at a fixed budget, and a bounded LLM assistant
that clears a 26-case golden gate at $0.92$ grounding with no
guilt-attributing output released. Code, configs, seeds, five datasheets, a
model card and a number-to-config map accompany the paper. The system triages;
it does not decide guilt.
\end{abstract}

\begin{IEEEkeywords}
graph neural networks, anomaly detection, anti-money laundering, bid rigging,
class imbalance, explainability, transfer learning, evaluation protocol
\end{IEEEkeywords}

% =====================================================================
\section{Introduction}
% =====================================================================

Obligations for stand-alone high-risk systems under the EU AI Act now bite from
2 December 2027. They were originally due on 2 August 2026, and the Digital
Omnibus on AI (Regulation (EU) 2026/1744, in force 27 July 2026) pushed them
back~\cite{aiact,omnibus}. Moving the date does not soften the requirement.
Explainability, human oversight, logging and auditability become legal
properties of a deployed system, and a screening model has to be able to say
\emph{why} it raised a flag in terms a reviewer can check and a regulator can
audit.

Whether a given system falls inside that regime is subtler than it is usually
reported, and the detail matters here. Annex III~5(b) treats creditworthiness
assessment as high-risk but carves out systems built to detect financial
fraud~\cite{aiact}. The carve-out is narrower than it first appears. Commission
guidance requires fraud detection to be the system's main intended purpose and
declines to stretch the exemption over anti-money-laundering or
counter-terrorist-financing work~\cite{eccguidance}. An AML screener of the kind
we describe therefore sits inside the regime that a pure fraud-scoring product
sits outside of, with roughly two years to get ready.

The deadline lands on two enforcement worlds that have never shared a toolchain.
Inside banks, anti-money-laundering (AML) teams run transaction-monitoring rules
that fire on individual records; industry estimates put the false-positive share
of those alerts at 90--95\%, and academic figures are comparable~\cite{fprate}.
Competition authorities and procurement auditors work the other side, checking
tenders by hand, one at a time, across a small sample, and usually learning
about a cartel only afterwards through a complaint or a leniency application.

The two worlds are closer than their literatures suggest. One wire transfer of a
modest sum is unremarkable. Twelve accounts passing the same money around a
circle inside 72 hours is laundering. One bid is procedurally clean: the tender
was advertised, bids arrived, a winner was declared. Five firms that quietly
take turns winning, price their losing bids in an implausibly tight band, and
turn out to share directors and a registered address are a cartel. The
individual record is innocent in both settings and the network is not.
Participants in both keep every record under whatever threshold the rules check,
which pushes the offence up a level into the topology. Confirmed positives, in
both, are rare against a very large volume of ordinary activity.

Fig.~\ref{fig:ledgers} makes the correspondence concrete. Five mechanisms recur
across the two domains and each collapses to one graph signature. Laundering
cycles and bid rotation are directed cycles. Smurfing and cover bidding are
fan-in stars. Layering and market allocation are fan-out stars. Shell-company
chains and shared directorships are dense co-attribute subgraphs. Pass-through
clusters and clustered bid prices are tight communities with odd edge
attributes. If the correspondence holds, one stack ought to serve both ledgers,
and the question of whether a learned sense of ``collusion structure'' survives
the crossing becomes something you can measure instead of assert.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig2_two_ledgers.png}
\caption{Two ledgers, one structure. Five collusion mechanisms, the graph
signature each leaves behind, and the domain-specific name it goes by on
either side. The correspondence is the premise of the shared stack: only the
adapters and the red-flag vocabularies differ below it.}
\label{fig:ledgers}
\end{figure*}

\subsection{Research questions}

\begin{itemize}
\item[\RQ{1}] Can graph neural networks combined with unsupervised graph
anomaly detection identify illicit-finance and bid-rigging structures with
materially higher precision \emph{at fixed alert budgets} than rule-based and
tabular machine-learning baselines?
\item[\RQ{2}] Do collusion fingerprints learned from confirmed cases,
augmented with synthetically injected patterns, transfer to unseen markets and
unseen typologies at a controlled flag rate?
\item[\RQ{3}] Can subgraph-level explanation methods produce alert rationales
that align with the red-flag frameworks practitioners already use?
\item[\RQ{4}] To what extent can one shared stack serve both domains, and does
a representation of collusion structure learned in one provide usable signal
in the other?
\end{itemize}

\subsection{Contributions}

\begin{enumerate}
\item \textbf{A unified intermediate representation and six adapters} covering
both domains, with the awkward parts of each dataset written down instead of
patched over: missing bidder identities, case-control sampling, a post-window
label spike.
\item \textbf{A budget-first, leakage-safe evaluation protocol} built on strict
inductive splits, frozen train-time normalisation, tie-aware precision@$k$,
multi-seed reporting and paired stratified bootstrap significance. We count the
protocol itself as a contribution, since the recent critiques of graph-anomaly
evaluation are implemented here and not simply cited.
\item \textbf{Transfer matrices at both granularities and in both directions},
with label-efficiency curves that pin down where cross-domain transfer stops
paying for itself.
\item \textbf{An explanation layer with an instrument attached}: PGExplainer
subgraphs, a nine-motif matcher, FATF and OECD citations, a practitioner study
protocol, and a bounded copilot gated on what it actually releases.
\item \textbf{A catalogue of negative results}, among them four separate
measurements showing that validation selection cannot see the Elliptic
distribution shift, and a joint cross-crime experiment that we specify in full
and have not run (Section~\ref{sec:joint}).
\end{enumerate}

% =====================================================================
\section{Related Work}
% =====================================================================

\textbf{Graph anomaly detection and benchmarks.} The survey by Ma et
al.~\cite{ma2023} supplies the node/edge/subgraph taxonomy we work inside.
GADBench~\cite{gadbench} is the uncomfortable reference point: over 29 models
and ten datasets, tree ensembles with plain neighbourhood aggregation match or
beat graph neural networks purpose-built for the task. Our \RQ{1} result
reproduces that ordering on Elliptic++, under a protocol strict enough that we
think the reproduction is worth reporting on its own.

\textbf{AML with graph neural networks.} Motie and Raahemi~\cite{motie2024}
survey the field. LineMVGNN~\cite{linemv2025} adds a line-graph view to two-way
message passing so that direction of money flow is preserved. Lawal et
al.~\cite{lawal2025} run GNNExplainer over Elliptic and write mainly about what
fails, showing how imbalance, sparse structure and noisy labels together block
reliable learning; that is the clearest published warning that headline accuracy
means little at this prevalence. The AMLworld line~\cite{multignn} finds
bidirectional message passing decisive, which we reproduce independently at
$-0.194$ AUC-PR once reverse edges are removed, the largest single-component
effect we measured. Near-perfect Elliptic numbers are best read against the
ablations in the same papers: one wavelet-temporal graph
transformer~\cite{lin2026} reports about $0.98$ accuracy while conceding that
its wavelet features contribute roughly $0.01$ and are ``not strictly
indispensable''.

\textbf{Cartel detection.} The screens tradition of coefficient of variation,
price spread and kurtosis is long established, and the benchmarks that get
reused come from Garc\'ia Rodr\'iguez et al.~\cite{garcia2022} and the Mendeley
EU cartel release~\cite{mendeley2025}. Recent cartel GNN work posts strong
cross-market numbers. Graph attention networks over tender--firm graphs spanning
13 markets in seven countries transfer at 80--90\% accuracy, peaking at 91\% on
an eight-market configuration and settling at 84\% once extended to
12~\cite{imhof2025}. Relational GCNs with an explicit zero-shot phase attack
out-of-distribution generalisation head on~\cite{gomes2024}, and a systematic
mapping study names the real-world-validation gap~\cite{epjds2025}. We want to
be careful here: those accuracy figures and our budget-first lift numbers are
different metrics computed on differently sampled data, so we make no claim to
beat them. What our matrices contribute is the shape of the degradation. It is
uneven across markets, and on the harder of the two benchmarks the market that
fails is the biggest one.

\textbf{Evaluation-protocol critiques.} A recurring complaint is that
graph-anomaly results are inflated by leakage, by transductive splits, and by a
mismatch between the unit that gets scored and the unit a human reviews. Our
transaction-versus-actor disagreement in Section~\ref{sec:granularity} measures
the third of those directly.

\textbf{Transfer and graph foundation models.} The graph-foundation-model
literature asks whether one representation can serve many graphs. \RQ{4} puts
that question to two enforcement domains in a form that can come back negative,
and in one direction it does.

\textbf{LLM-augmented explanation.} Several groups now pair language models with
graph explanations to produce narratives an analyst can read. We kept ours on a
short leash: read-only SQL over published artifacts, a numeric sanity gate, and
a guard on guilt-attributing language. The aim is an assistant that helps a
reviewer participate without inviting them to defer.

% =====================================================================
\section{Data and Unified Representation}
% =====================================================================

\subsection{Datasets}

Table~\ref{tab:data} lists the five public datasets, in the six graph views our
six adapters build from them (Elliptic++ supplies both a transaction graph and
an actor graph, and the disagreement between those two is itself a result; see
Section~\ref{sec:granularity}). Two views are labelled anchors carrying the
headline results. The rest supply scale, controllable ground truth, and an
unlabelled population for injection studies. Every dataset is
downloaded from its canonical source and verified against a committed
checksum manifest that also records its licence. No raw data is redistributed
in the release.

\begin{table*}[t]
\caption{Five public datasets in the six graph views the adapters build.
Roles: \textsc{anchor} carries headline results, \textsc{scale} stresses the
pipeline, \textsc{population} supplies an unlabelled portfolio for injection.
Positives are quoted in each dataset's own unit --- see
Section~\ref{sec:prevalence} for which denominator each evaluation uses.}
\label{tab:data}
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llllll}
\toprule
Dataset & Domain & Scale & Positives & Role & Licence \\
\midrule
Elliptic++ (tx graph)~\cite{elliptic,weber2019} & Financial & 203,769 nodes, 234,355 edges, 49 steps & $\approx$2\% illicit & \textsc{anchor} & research use \\
Elliptic++ (actor graph)~\cite{elliptic} & Financial & 292,102 test nodes, 966,352 edges & 5.3\% prevalence & granularity & research use \\
IBM AMLworld HI-Small & Financial & 5,078,345 tx, 515,080 accounts & 0.102\% laundering & \textsc{scale} & CDLA-Sharing-1.0 \\
Mendeley EU cartel & Procurement & 15,616 contracts, 7 countries, 2004--21 & 41.9\% (case-control) & \textsc{anchor} & CC BY-NC 3.0 \\
Garc\'ia Rodr\'iguez et al. & Procurement & 64,348 bids, 9,781 tenders, 6 markets & 11.3--81.8\% by market & transfer & CC BY-NC-ND 4.0 \\
OCDS Georgia (OpenTender) & Procurement & 163,327-node window & unlabelled & \textsc{population} & CC BY-NC-SA 4.0 \\
\bottomrule
\end{tabular}
\end{table*}

\subsection{The intermediate representation}

Every adapter emits the same typed structure: nodes, directed attributed edges,
labels carrying provenance and confidence, and a history pack that supports
as-of feature computation. Everything downstream is then domain-agnostic. The
contract also forbids cleaning heroics. An adapter may not impute a missing
identity, invent an edge, or quietly drop an inconvenient property of the
source. Three such properties survive into the representation as a result.

\begin{itemize}
\item \textbf{Garc\'ia bidder identities exist on four of six markets.} The
company-identifier column appears only in the per-market files for Japan, Italy,
Brazil and the USA. The combined file drops it, and the two Swiss markets never
carried it at all. Co-bidding \emph{identity} graphs are buildable on four
markets; the rest support only the anonymous bid-price tier.
\item \textbf{Mendeley is a case-control research sample}, not a population
file, and 41.9\% of its contracts are cartel-linked. Raw AUC-PR therefore
cannot be compared across its folds, whose prevalences run from $0.29$ to
$0.89$. Throughout this paper the cross-fold comparator is lift over fold
prevalence, never raw AUC-PR.
\item \textbf{AMLworld has a post-window label spike.} Of the transactions
dated after 10 September, 1,108 in number, 59.1\% are laundering, some 580
times the base rate. Our temporal splitter fences that tail off explicitly. A
naive split would produce a spectacular number and a meaningless one.
\end{itemize}

\subsection{A note on which prevalence is being quoted}
\label{sec:prevalence}

Three different denominators circulate in the literature on these datasets.
Mixing them up is an easy way to make a result look better or worse than it is,
so we fix the convention here and keep it for the rest of the paper.

Take Elliptic++ first. About 2\% of all transactions are labelled illicit, but
77\% carry no label whatsoever. We compute every metric over confirmed nodes
only, and in the test window of steps 35--49 that confirmed-only prevalence is
$0.065$. This is the baseline our AUC-PR should be read against and the figure
quoted in Table~\ref{tab:t1}. The 2\% describes the dataset; $0.065$ describes
the evaluation.

AMLworld splits by unit instead of by labelling. Laundering accounts for
0.102\% of transactions, while account-level node prevalence in our test split
is $0.0104$. Since our baselines score accounts, $0.0104$ is the number that
matters in Section~\ref{sec:robustness}.

Mendeley splits the same way. Cartel-linked contracts make up 41.9\% of the
case-control sample; firm-level test prevalence is $0.358$. Every Mendeley
result we report is firm-level.

\subsection{As-of label and feature policies}

Any feature computed from a node's history is computed as of the split
boundary, never across the full record. We treat this as a contribution and not
an implementation footnote, because history-derived features are where temporal
leakage prefers to hide. Aggregate statistics will not reveal it. The only
symptom is a validation score that looks a little too good. A dedicated leakage
suite enforces the policies and runs as its own continuous-integration step on
every push.

% =====================================================================
\section{Method}
% =====================================================================

Fig.~\ref{fig:arch} shows the whole system. One design rule governs it: a hard
trust boundary. The offline pipeline writes plain files and exits. Everything
on the user-facing side is read-only and can neither retrain a model nor alter
a score.

\begin{figure*}[t]
\centering
\includegraphics[width=0.94\textwidth]{fig1_architecture.png}
\caption{The shared stack. Stages 1--7 run offline and terminate, writing
Parquet and JSON artifacts; stages 8--9 serve those artifacts read-only. The
same nine stages run for both domains; only the ingestion adapters and the
red-flag vocabularies differ.}
\label{fig:arch}
\end{figure*}

\subsection{Detection arms}

Four arms score every node and are fused afterwards.

\textbf{Baselines.} B1 is a rule engine in the spirit of the threshold logic
banks actually deploy. B2 is XGBoost on node features. B3 is XGBoost on node
features together with graph-derived ones, meaning degrees, k-core, motif counts
and neighbourhood aggregates. B3 follows the GADBench recipe and is the yardstick
that matters. On the procurement side we add B4, the classical screens tier of
coefficient of variation, price spread, kurtosis and relative distance.

\textbf{Supervised graph neural networks.} GraphSAGE~\cite{sage},
GATv2~\cite{gatv2} and R-GCN~\cite{rgcn}, all direction-aware: the graph is
built with explicit reverse edges so that message passing can distinguish
sending from receiving. Focal loss~\cite{focal} is the default given the
imbalance; weighted cross-entropy is the ablation arm.

\textbf{Unsupervised arm.} DOMINANT~\cite{dominant} and a graph autoencoder
score structural reconstruction error, plus a deterministic structural floor.
These require no labels and are the only arms available on the unlabelled OCDS
population~\cite{ocds}.

\textbf{Fusion.} We isotonically calibrate each member on validation before
fusing, so the combined score behaves like a comparable probability instead of
an arbitrary blend of ranks. The gap between calibrated and rank fusion turned
out to be one of the largest effects we measured anywhere in the paper.

\subsection{From node scores to an alert queue}

What limits a compliance team is investigator hours, not model output, so the
operational unit has to be a group and not a node. Leiden community
detection~\cite{leiden} over the scored graph proposes the candidates. We then
rank groups by aggregate member risk, deduplicate them with Jaccard non-maximum
suppression, cap their size, and cut the list at a fixed review budget. A group
counts as a hit when at least one member is confirmed, tie-aware.
Section~\ref{sec:robustness} sweeps every parameter in that pipeline.

\subsection{Explanation layer}

For each alert the system returns four things: the minimal responsible subgraph
from PGExplainer~\cite{pgexplainer}, which we adopted over
GNNExplainer~\cite{gnnexplainer} after the fidelity ablation in
Section~\ref{sec:rq3}; the matched motif; the attributes supporting it; and the
particular FATF or OECD red-flag indicator that the shape corresponds to. An
alert that arrives without an explanation does not count as delivered.

\subsection{Guarded copilot}

A bounded language-model agent answers analyst questions over the published
artifacts. It is constrained by construction: a SELECT-only allowlist over
read-only views, a numeric sanity gate that rejects any figure not present in
the underlying rows, a guilt-language guard, and an immutable caveat on every
response. Its release gate is a golden set of 26 cases graded for grounding
and for released-output violations, with a zero-tolerance bar on what ships and
a separate ceiling on draft rewrites.

\subsection{Evaluation protocol}
\label{sec:protocol}

Each clause below changed a headline number at some point while we were
building the system, which is why we treat the protocol as a result in its own
right.

\begin{itemize}
\item \textbf{Strict inductive splits.} Test nodes and their edges stay unseen
during training. Temporal splits fence the boundary; entity splits (LOCO, LOMO)
hold out whole countries or markets.
\item \textbf{Frozen train-time normalisation.} We compute feature statistics
on train and freeze them. Recomputing on test amounts to inadvertent test-time
adaptation, and removing it pulled our own headline GATv2 number down from
$0.693$ to $0.532$, widening the true gap to the tree baseline. Every figure we
report is post-fix.
\item \textbf{Budget-first, tie-aware metrics.} Precision@$k$ at budgets a real
team would work, with ties handled explicitly, alongside AUC-PR, which is the
right summary under this degree of imbalance~\cite{saito2015} and is always
quoted against its own fold's prevalence baseline.
\item \textbf{Multi-seed with dispersion.} Five seeds, reported as
mean~$\pm$~standard deviation. Seed variance of about $\pm 0.05$ swamps
cross-machine variance of about $\pm 0.02$, which makes a single-seed comparison
across two machines uninterpretable. Our own previously published seed-0 GATv2
figure of $0.5492$ happens to be the best of the five. The defensible number is
$0.4729 \pm 0.0525$, and that is the one we quote.
\item \textbf{Paired stratified bootstrap} over identical confirmed test nodes
for every headline comparison, at 2,000 resamples.
\end{itemize}

% =====================================================================
\section{Results}
% =====================================================================

\subsection{RQ1: trees still win, and multi-seed sharpens it}

Table~\ref{tab:t1} gives the Elliptic++ scoreboard. Gradient-boosted trees on
graph features reach $0.8104$ AUC-PR against a prevalence baseline of
$0.065$. The best graph neural network we trained reaches
$0.4729 \pm 0.0525$. The gap is not a tuning artifact of one seed:
Table~\ref{tab:t3} puts the paired bootstrap delta at $-0.261$ with a 95\%
confidence interval of $[-0.285, -0.235]$ and $p < 0.001$.

\begin{table}[t]
\caption{Elliptic++ node-level results (test steps 35--49, prevalence
$0.065$). Rule and tree baselines are deterministic; GNN and ensemble rows are
5-seed mean $\pm$ standard deviation.}
\label{tab:t1}
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lll}
\toprule
Model & AUC-PR & P@100 \\
\midrule
B1 rules & 0.0576 (det.) & 0.00 \\
B2 XGBoost & 0.8076 (det.) & 1.00 \\
\textbf{B3 XGBoost + graph} & \textbf{0.8104} (det.) & \textbf{1.00} \\
GATv2-focal & $0.4729 \pm 0.0525$ & $0.8120 \pm 0.2383$ \\
GATv2-weighted CE & $0.4388 \pm 0.0505$ & $0.7240 \pm 0.1466$ \\
DOMINANT & $0.0410 \pm 0.0003$ & --- \\
Ensemble, calibrated & $0.4434 \pm 0.0501$ & --- \\
Ensemble, rank & $0.0511 \pm 0.0019$ & --- \\
Structural floor & $0.0547 \pm 0.0000$ & --- \\
GAE & $0.0386 \pm 0.0001$ & --- \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\caption{Paired-bootstrap comparisons over identical confirmed test nodes
($n = 16{,}670$; 2,000 stratified resamples; seed-0 score files, so these
deltas measure sampling uncertainty at fixed seed and are not
differences of multi-seed means). Both $p$ values sit at the resolution floor
of 2,000 resamples under add-one smoothing ($2/2001 \approx 0.001$), so they
should be read as $p < 0.001$ rather than as point estimates.}
\label{tab:t3}
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrr}
\toprule
Comparison & $\Delta$ AUC-PR & 95\% CI & $p$ \\
\midrule
GATv2-focal vs.\ B3 & $-0.261$ & $[-0.285, -0.235]$ & $<0.001$ \\
Calibrated vs.\ rank & $+0.471$ & $[0.440, 0.499]$ & $<0.001$ \\
\bottomrule
\end{tabular}
\end{table}

Two points deserve separating. The tree baseline is not winning because the
graph carries no information. B3 beats B2 by only $0.8104$ to $0.8076$, so most
of its strength sits in the node features, and what the deep models fail at is
converting structure into generalisation once the distribution moves. The
second point is arguably the more useful one for practitioners: the biggest
effect in Table~\ref{tab:t3} has nothing to do with model choice. Calibrating
ensemble members before fusing them buys $+0.471$ AUC-PR over rank fusion,
roughly an order of magnitude more than any architectural decision we tried.

Fig.~\ref{fig:crater} explains where the graph model's average goes. Through
step 42 the model tracks well above prevalence; from step 43 onward it
collapses to the floor. Step 43 is the point at which a dark-market closure
enters the Elliptic timeline, a discontinuity noted since the dataset's
original release~\cite{weber2019}: the test window contains a regime the
training window never saw.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig3_timestep_crater.png}
\caption{Per-time-step AUC-PR across the Elliptic++ test window, showing the
5-seed mean and one standard deviation. The model holds up well through step 42,
then drops to the prevalence floor at step 43 where the market closure lands.
Quote a single aggregate figure and none of this is visible.}
\label{fig:crater}
\end{figure}

The procurement anchor in Table~\ref{tab:t2} preserves the ordering and makes
the graph model look worse still. R-GCN scores $0.2808 \pm 0.0087$ against a
prevalence of $0.358$: stable across seeds, and below what guessing would give
you. Our best firm-level configuration is a tree with the publisher's own
precomputed screens bolted on, at $0.4558$. The uncomfortable headline for this
dataset is that the screens tradition contributes more than the graph model
does.

\begin{table}[t]
\caption{Mendeley EU firm-level results (within the case-control sample,
prevalence $0.358$). Lift-style reading applies. ``+screens'' rows add the
publisher's own precomputed screen columns to that baseline's inputs.}
\label{tab:t2}
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lll}
\toprule
Model & AUC-PR & P@18 \\
\midrule
B1 rules & 0.3426 (det.) & 0.47 \\
B2 XGBoost & 0.3925 (det.) & 0.22 \\
B3 XGBoost + graph & 0.3775 (det.) & 0.56 \\
B4 screens & 0.3811 (det.) & 0.78 \\
\textbf{B2 + screens} & \textbf{0.4558} (det.) & 0.61 \\
B3 + screens & 0.3450 (det.) & 0.39 \\
B4 + screens & 0.3874 (det.) & 0.50 \\
R-GCN focal (5 seeds) & $0.2808 \pm 0.0087$ & $0.0746 \pm 0.0363$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Queue granularity: the unit you score is not the unit you review}
\label{sec:granularity}

Scoring transactions and scoring actors turn out to be different problems, and
the difference shows up in the numbers. On the Elliptic++ actor graph, R-GCN
reaches $0.2473$ AUC-PR against a prevalence of $0.053$, a lift of $4.7\times$,
and the head of its ranking is remarkably dependable: precision@100 of $0.98$,
stable across seeds. Roll those node scores up into review groups, though, and
the queue falls apart. The 2,514 actor alerts give precision@50 of $0.08$.

The transaction-level queue runs the other way. Its 318 communities condense
into 254 alerts at precision@50 of $0.32$, precision@100 of $0.23$ and
precision@200 of $0.135$, a triage list an analyst could work, covering 7.8\% of
the illicit population in its top 50. Procurement yields 223 alerts from 1,283
communities at precision@4 of $0.50$.

The lesson for practice is that node-level ranking quality and queue-level
precision are not substitutes for one another. Report only the first and you may
be describing a product nobody can use. We report both.

\subsection{RQ2: injection recovery separates flow motifs from award motifs}
\label{sec:injection}

Because confirmed cartels are scarce, we plant known motifs into the
unlabelled 163,327-node OCDS Georgia window and measure recovery at a fixed
budget. Table~\ref{tab:t6} is the result, and it splits cleanly.

\begin{table}[t]
\caption{Injected-member recall@2000 on the OCDS Georgia window (population
163,327; 5 seeds, mean $\pm$ s.d.; rank-fusion unlabelled mode --- the
unsupervised arms are the only ones available without labels). Note the wide
dispersion on the two recovered families: the seed-to-seed spread is larger
than several of the between-method gaps.}
\label{tab:t6}
\centering
\footnotesize
\setlength{\tabcolsep}{2.5pt}
\begin{tabular}{lllll}
\toprule
Motif family & DOMINANT & Ens.\ (rank) & Floor & GAE \\
\midrule
Coordinated cluster & $0.37 \pm 0.15$ & $\mathbf{0.92 \pm 0.17}$ & $0.12 \pm 0.00$ & $0.91 \pm 0.12$ \\
Common control & $\mathbf{0.58 \pm 0.04}$ & $0.44 \pm 0.02$ & $0.43 \pm 0.00$ & $0.47 \pm 0.02$ \\
Partition & $0.17 \pm 0.01$ & $0.03 \pm 0.03$ & $0.00 \pm 0.00$ & $0.05 \pm 0.01$ \\
Rotation & $0.08 \pm 0.01$ & $0.00 \pm 0.00$ & $0.00 \pm 0.00$ & $0.04 \pm 0.01$ \\
Cover bid & $0.01 \pm 0.01$ & $0.00 \pm 0.00$ & $0.00 \pm 0.00$ & $0.01 \pm 0.01$ \\
\bottomrule
\end{tabular}
\end{table}

Motifs that alter the shape of the graph come back: coordinated clusters at
$0.92$ recall, common control at $0.58$. Motifs that alter only who wins stay
nearly invisible. Rotation, cover bidding and market partition never exceed
$0.17$ under any method we tried. No amount of tuning will fix this, because the
limitation is structural. Bid rotation leaves the co-bidding topology almost
exactly as it found it and moves only the award labels along the edges.
Catching it calls for award-sequence features, not deeper message passing. We
think this is among the more useful negatives we report, given how often the
cartel literature treats rotation as a flagship detectable pattern.

A caveat belongs with that headline. Coordinated-cluster recall carries a
standard deviation of $0.17$ over five seeds, so the apparent ordering between
the rank ensemble at $0.92$ and the graph autoencoder at $0.91$ sits well inside
noise. What the experiment supports is the family-level split between
shape-changing and award-changing motifs. It does not support a ranking of
unsupervised detectors, and we do not offer one.

\subsection{RQ4: transfer is real, market-dependent, and asymmetric}

Fig.~\ref{fig:transfer} and Table~\ref{tab:t4} give the two procurement
transfer matrices. Held-out prevalences vary from $0.25$ to $0.89$, so lift
over fold prevalence is the only fair comparator.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig4_transfer_lift.png}
\caption{Transfer lift by held-out fold. Every Garc\'ia market comes out
positive under leave-one-market-out. Mendeley's leave-one-country-out folds
average $1.17$ and yet the largest market fails. A bar at or below $1.0$ has
done no better than the fold's own base rate.}
\label{fig:transfer}
\end{figure}

\begin{table}[t]
\caption{Procurement transfer matrices, 5 seeds per fold. Lift is AUC-PR
divided by the held-out fold's own prevalence; $1.0$ means no better than the
fold's base rate. Mendeley LOCO macro lift $1.17$; Garc\'ia LOMO macro lift
$1.57$.}
\label{tab:t4}
\centering
\small
\begin{tabular}{lrrr}
\toprule
Held-out fold & $n$ & AUC-PR & Lift \\
\midrule
\multicolumn{4}{l}{\textit{Mendeley EU --- leave-one-country-out}} \\
\quad country\_1 & 30 & $0.8664 \pm 0.0224$ & 1.13 \\
\quad country\_2 & 750 & $0.6464 \pm 0.0182$ & \textbf{0.90} \\
\quad country\_3 & 63 & $0.4983 \pm 0.0603$ & 1.74 \\
\quad country\_4 & 76 & $0.7427 \pm 0.0812$ & 1.03 \\
\quad country\_5 & 60 & $0.7630 \pm 0.0473$ & 1.14 \\
\quad country\_6 & 9 & $0.9527 \pm 0.0346$ & 1.07 \\
\quad country\_7 & 13 & $0.8091 \pm 0.0060$ & 1.17 \\
\midrule
\multicolumn{4}{l}{\textit{Garc\'ia Rodr\'iguez --- leave-one-market-out}} \\
\quad America & 120 & $0.3894 \pm 0.1080$ & 1.56 \\
\quad Brazil & 272 & $0.5063 \pm 0.1161$ & 1.77 \\
\quad Italy & 821 & $0.7858 \pm 0.0585$ & 1.43 \\
\quad Japan & 1665 & $0.4295 \pm 0.0223$ & 1.53 \\
\bottomrule
\end{tabular}
\end{table}

Garc\'ia leave-one-market-out comes out positive everywhere. Every held-out
market beats its own base rate, the macro lift is $1.57$, and the Italy fold is
strong enough that a three-alert queue over it returns three confirmed cartels.
Mendeley leave-one-country-out does not behave nearly so well. Small folds carry
the macro lift of $1.17$, while \texttt{country\_2}, which holds 750 firms and
is by a wide margin the largest fold, lands at $0.90$ and does worse than its
own base rate. Averaging across folds this heterogeneous flatters the result. Had
we reported the macro figure alone, a genuine limitation would have read as a
success.

\textbf{Cross-domain.} Freeze an encoder trained on one domain, probe it on the
other, and the two directions disagree. Procurement to financial reaches
$0.1502$ against a prevalence of $0.065$, a lift of $2.31\times$. Financial to
procurement reaches $0.2841$ against a prevalence of $0.358$, which is below the
base rate and therefore no signal at all.

\textbf{And it only pays when labels are plentiful.} Fig.~\ref{fig:labeleff}
sweeps how many target-domain labels are available. Below roughly a thousand,
almost every budget in both directions shows a negative transfer gain: the
frozen source probe does worse than simply training on the raw target features
already to hand. Transfer here is a convenience once you have scale. It is not a
cold-start solution, which is a shame, because cold start is when a practitioner
would want it most.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig6_label_efficiency.png}
\caption{Cross-domain label efficiency. Each bar gives the gain of a frozen
source-domain probe over a raw-feature probe at the same label budget. Below
$k \approx 1000$ almost every bar is negative, which is to say transfer does
nothing for the cold-start problem in either direction.}
\label{fig:labeleff}
\end{figure}

\subsection{Robustness, ablations, and a sharp negative}
\label{sec:robustness}

Table~\ref{tab:t7} collects the component ablations, and two conventions govern
how to read it. A ``$-$'' row takes a component out of the adopted model, so its
delta is the cost of removal. A ``$+$'' row adds a variant we evaluated and did
not adopt, so its delta is a rejection margin. Those are different quantities
and should not be compared with one another; only the ``$-$'' rows can support a
claim about which component matters most. Each delta is measured against the
reference sharing its basis, and we never difference a seed-0 result against a
multi-seed mean.

\begin{table}[t]
\caption{Component ablations. ``$-$'' rows are ablation costs; ``$+$'' rows
are rejection margins for variants that were measured and not adopted.}
\label{tab:t7}
\centering
\footnotesize
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{llll}
\toprule
Arm & Basis & AUC-PR & $\Delta$ \\
\midrule
GATv2-focal (headline) & 5 seeds & $0.4729 \pm 0.0525$ & ref. \\
\quad $-$ focal loss & 5 seeds & $0.4388 \pm 0.0505$ & $-0.0341$ \\
Calibrated ensemble & 5 seeds & $0.4434 \pm 0.0501$ & ref. \\
\quad $-$ unsup.\ members & 5 seeds & $0.4729 \pm 0.0525$ & $+0.0295$ \\
GATv2-focal (headline) & seed 0 & $0.5492$ & ref. \\
\quad $-$ bidir.\ edges & seed 0 & $0.3549$ & $\mathbf{-0.1943}$ \\
\quad $+$ structural chan. & seed 0 & $0.3781$ & $-0.1711$ \\
\quad $+$ line-graph chan. & seed 0 & $0.4986$ & $-0.0506$ \\
\quad $+$ context fusion & seed 0 & $0.3242$ & $-0.2250$ \\
Mendeley B2 XGBoost & det. & $0.3925$ & ref. \\
\quad $+$ dataset screens & det. & $0.4558$ & $+0.0633$ \\
\bottomrule
\end{tabular}
\end{table}

Direction-awareness is the component that earns its place. Dropping reverse
edges costs $0.194$ AUC-PR, which reproduces the AMLworld finding on a different
dataset and by a different team. Focal loss is worth a real but second-order
$0.034$. Extra input channels hurt us every time we tried them: structural,
line-graph and context-fusion arms all landed below raw features alone. We read
that as an interaction between model capacity and distribution shift, not as
evidence that those channels carry no information in general.

\textbf{Protocol sensitivity, and where it is not flat.} We swept the Jaccard
suppression threshold over $\{0.3, 0.4, 0.5, 0.6, 0.7\}$ crossed with three
hit-rule settings. The two axes do not behave alike, and rolling them into a
single robustness claim would misrepresent both.

Suppression leaves the alert set entirely alone. At every threshold the
financial queue keeps all 254 alerts and the procurement queue all 223, with
none suppressed anywhere in the grid. The communities barely overlap, so
non-maximum suppression never binds, and the published queues owe nothing to a
tuned deduplication setting.

The hit rule is a different story. Our published queue counts a group as a hit
when at least one member is confirmed. Require instead that a confirmed fraction
of $0.1$ of the members be confirmed and financial precision@50 falls from
$0.32$ to $0.28$, with P@100 going $0.23$ to $0.21$ and P@200 $0.135$ to
$0.125$. Tightening further to $0.25$ costs nothing beyond that. Procurement
stays flat on both axes, holding P@18 at $0.33$ throughout. So about $0.04$ of
our headline queue precision rests on a definitional choice. We report the
looser rule and state the dependence, since presenting the number as though no
choice were involved would not be true.

\textbf{The label-noise result.} Fig.~\ref{fig:noise} carries the sharpest
negative in the paper. We flipped a fraction of the training labels at random
and tracked validation and test AUC-PR together. Test performance climbed
steadily, from $0.4827 \pm 0.0616$ on clean labels to $0.5978 \pm 0.0248$ with
20\% flipped. Validation fell over the same range, $0.9418$ down to $0.5692$.
The curves move apart.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig5_label_noise.png}
\caption{Label-noise robustness on Elliptic++ (GATv2-focal, 3 seeds per rate).
Corrupting training labels \emph{improves} test AUC-PR while validation
collapses. Model selection on validation is optimising against the wrong
curve.}
\label{fig:noise}
\end{figure}

The explanation is that validation sits entirely in the pre-shift regime while
test straddles the step-43 break. Noise in the labels acts as a regulariser,
stopping the model from committing too hard to pre-shift structure. That costs
validation and buys test. This is the fourth independent sign we have that
validation selection cannot see this shift, alongside the frozen-normalisation
fix, the three added-channel degradations, and the per-step crater in
Fig.~\ref{fig:crater}.

We should be plain about what this does and does not license. It is a
diagnostic. Nobody should go and corrupt their labels. What it tells us is that
the standard model-selection loop becomes unreliable under temporal shift, and
that a paper quoting only validation-selected test numbers on Elliptic is
reporting luck at least as much as skill.

\textbf{AMLworld at scale.} For completeness we report a negative at 5M edges:
tree baselines score $0.0064$ (B2) and $0.0062$ (B3) against an account-level
prevalence of $0.0104$ (Section~\ref{sec:prevalence}), which puts both below
chance. The diagnosis is structural. AMLworld labels edges, not nodes, and a
node-first-seen split leaves test accounts with a median degree of 3 against 13
in train, so there is hardly any structure left for a model to score. We
therefore use AMLworld for what it was designed to do, injection against known
ground truth, and not as a supervised benchmark.

% =====================================================================
\section{Explanation Study}
\label{sec:rq3}
% =====================================================================

\textbf{Choosing an attribution method.} We ran a three-arm ablation over 50
alerts, comparing GNNExplainer, PGExplainer and raw attention. PGExplainer
returned sane attributions on 49 of the 50 nodes where GNNExplainer managed 12,
and did so in under half the time, 51.3\,s against 122.9\,s. We adopted it.
Regenerating the bundles afterwards cut implausible-fidelity explanations from
38 of 50 down to 1. Attention weights, popular precisely because they look like
interpretability for free, matched GNNExplainer's weaker sanity rate.

\textbf{Motif matching and red-flag mapping.} A rule-based matcher emitting
nine motif types (cycle, fan-in, fan-out, pass-through, common control,
rotation, cover bid, partition, clique) runs over every alert's subgraph. It is
implemented independently of the injection generators so the two cross-validate
each other, and a parameterised test asserts that it recovers all ten generator
families, five financial and five procurement, at 100\% recall on fixtures.
Coverage on real queues is patchier. On the financial queue 15 of 50 bundles
carry a proven motif with FATF citations attached, and on the actor queue 15 of
20. On the procurement queues the count is zero. The Mendeley alert communities
and the three Garc\'ia Italy alerts are simply too small, often just two firms,
to contain a shape the matcher can prove. That is a coverage limitation and we
would rather record it than paper over it with a weaker claim.

\textbf{Copilot gate.} The release gate grades 26 golden cases against two
separate bars. The first is zero-tolerance and applies to released output: no
guilt-attributing language may ship, and a deterministic guard rather than a
prompt enforces it. The second is a $0.9$ threshold on grounding, under which
every number the assistant quotes must appear in the rows it actually read. The
gate passes. Grounding comes in at $0.923$, or 24 of 26; released guilt
violations at zero; draft rewrites at 1 of 26 against a $10\%$ ceiling.

We report the two grounding misses instead of rounding them away, because they
are not scattered at random. Both are the newest cases in the set and both
exercise the tool we added most recently, the injection-study reader. One quoted
a recall figure absent from the rows it had retrieved; the other dropped an
expected motif name from an answer that was otherwise well grounded. Growing the
golden set from 24 to 26 left the older cases untouched and broke the new ones.
For a bounded agent whose tool surface has just widened, that is the failure you
would predict, and it argues for extending the golden set with every new tool
instead of leaning on a past perfect score.

Two earlier failures were fixed with prompt rules and not model changes. The
agent once echoed a guilt-presupposing question in its own words while denying
the premise, and once reformatted a metric from $0.32$ to ``32\%'' in a way that
broke traceability back to the artifact. What holds the released-output bar is
the deterministic guard, not the model's willingness to comply.

\textbf{Practitioner phase.} The instrument, the rater packets and the scoring
rubric are built and released. \textbf{The human study has not been run.} We
report the protocol and record the absence of practitioner judgements as a
limitation; substituting our own judgement for theirs would defeat the point of
asking. Our pre-registered fallback is a panel of rubric-trained graduate raters
including at least one domain expert, reported as exactly that.

% =====================================================================
\section{A Joint Cross-Crime Extension}
\label{sec:joint}
% =====================================================================

\noindent\fbox{\parbox{0.95\columnwidth}{\small \textbf{Status: design and
protocol only.} This section specifies an experiment we have \emph{not run}. It
reports no results and contains no measured number. We include it because the
design falls out of the paper's premise, and because writing it down in advance
is what stops it becoming a post-hoc story later.}}

\medskip

Everything above treats the two domains as two evaluation surfaces for one
stack. The same architecture trains separately on each ledger, and \RQ{4} asks
whether a representation survives the trip between them. There is a blind spot
in that framing: it quietly assumes the offender populations do not overlap, as
though the launderer and the bid rigger were always different people.

They need not be. A firm winning rigged public contracts has proceeds to place,
and one beneficial owner can sit behind both the bidding entity and the accounts
that move the money. If that holds, the two ledgers stop being two datasets and
become two views of a single graph, stitched together by shared-control edges
that neither siloed detector can see at all (Fig.~\ref{fig:dual}).

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig7_dual_actor.png}
\caption{The joint cross-crime design. A dual actor appears in both ledgers
under common control. A joint heterogeneous model can traverse the
shared-control edge; two siloed detectors cannot observe it at all. The
comparison is specified here and has not yet been run.}
\label{fig:dual}
\end{figure}

\subsection{Hypothesis and design}

\textbf{H1.} On actors present in both ledgers, a joint model over the merged
heterogeneous graph achieves higher recall at a fixed alert budget than the
better of two siloed detectors run independently.

\textbf{H2.} That advantage concentrates in actors whose evidence is
sub-threshold in each ledger taken alone. This is the case the design exists
for, since an actor already flagged on one side needs no help from the other.

\textbf{Construction.} A merged graph with typed nodes for account, entity, firm
and tender, and typed edges for transfer, co-bid, award and shared control. An
R-GCN shares parameters across relation types, which makes shared control a
first-class edge type instead of a feature bolted on. For the siloed baseline we
train the same architecture per ledger with the shared-control edges deleted, so
what the comparison isolates is the join and not model capacity.

\textbf{Ground truth.} No public dataset links a confirmed cartel firm to a
confirmed laundering account, and that absence is the whole reason the
experiment remains unrun. The workable substitute is a dual-actor injection
study: plant paired motifs, a procurement rotation clique and a financial
layering chain, sharing a controlled entity, vary the fraction of dual actors,
then measure recall of the planted pairs under joint and siloed models. It
inherits the injection machinery validated in Section~\ref{sec:injection}, and
it inherits that machinery's weakness too. Award-pattern motifs recover poorly,
which should be expected to cap the procurement half of any dual-actor result.

\subsection{What would falsify it}

Should the joint and siloed models land within noise of each other on planted
dual actors, the cross-crime hypothesis buys nothing operationally and the
siloed framing wins. We think that outcome is quite possible, given that the
cross-domain transfer we did measure is lopsided, weak one way and absent the
other. Writing the design down in advance is what keeps that result publishable
if it happens.

% =====================================================================
\section{Regulatory Alignment and Ethics}
% =====================================================================

Section~I set out the timing. The EU AI Act's stand-alone high-risk obligations,
covering technical documentation, logging, human oversight and information good
enough for a deployer to interpret the output, apply from 2 December 2027 after
the Digital Omnibus deferral~\cite{aiact,omnibus}, and the Annex III~5(b)
carve-out for fraud detection is read too narrowly to cover AML or CFT
work~\cite{eccguidance}. In the UAE, Article~149 of CBUAE Federal Decree-Law
No.~6 of 2025 obliges licensed institutions to implement robust fraud detection
and to cooperate with the Central Bank on fraud data and
patterns~\cite{cbuae2025}. On the procurement side the OECD's 2025 guidelines
put public procurement at roughly 13\% of member-country GDP and estimate that
eliminating bid rigging could cut procurement prices by 20\% or
more~\cite{oecd2025}.

Our approach is to map each obligation onto something the system actually does.
Every alert ships with a subgraph, a matched motif and a red-flag citation,
which is the interpretability requirement made concrete. The queue stops at a
fixed budget, so the system is built as a triage instrument for a human reviewer
and could not act autonomously even if asked. Artifacts are immutable files and
the serving layer is read-only, so an auditor can reconstruct exactly what an
analyst saw. The caveat attaches at the API boundary where no client can
suppress it, and a release gate, not a prompt convention, enforces the
guilt-language guard.

\textbf{Scope boundary.} This system determines no legal guilt. It ranks cases
for human investigation. We target the maturity of a validated prototype, TRL
3--4, on public and synthetic data, and we used no personal or classified data
at any point. We say so in the paper for the same reason the system says so on
every response. A screening score misread as an accusation does real harm, and
the language guarding against that is part of the design.

% =====================================================================
\section{Limitations}
% =====================================================================

\begin{itemize}
\item \textbf{The Elliptic shift dominates the financial results}, and
validation selection cannot see it, as four separate measurements show. Read
test numbers on this benchmark as regime-dependent.
\item \textbf{Anonymised features limit what an explanation can say.} Elliptic's
features carry no names, so financial explanations stay structural.
Amount-level narratives exist only on AMLworld, which is synthetic.
\item \textbf{Mendeley is case-control.} Lift is the comparator, not raw AUC-PR,
and nothing about population-level precision follows from it.
\item \textbf{Garc\'ia identity coverage reaches four of six markets}, so
co-bidding identity graphs do not span the whole benchmark.
\item \textbf{Award-pattern motifs recover badly} (rotation, cover bidding,
partition), which caps what we can claim on the procurement side specifically.
\item \textbf{The copilot's grounding is imperfect and depends on its tool
surface.} Grounding stands at $0.923$ on the current 26-case gate, both misses
falling on the tool we added last. The released-output guarantee is
deterministic and holds at zero violations, but grounding is a statistical bar
and should be expected to dip whenever the agent gains tools faster than the
golden set grows.
\item \textbf{The practitioner study's human phase has not been run}, leaving
\RQ{3} resting on fidelity and coverage measurements instead of practitioner
judgement.
\item \textbf{The joint cross-crime experiment is unrun} (Section~\ref{sec:joint}).
\item \textbf{Compute envelope.} Everything published here ran on CPU,
full-batch. Neighbour-sampled minibatch training on the five-million-edge
AMLworld graph, and the PNA and GIN comparisons that would depend on it, sit
outside that envelope.
\end{itemize}

% =====================================================================
\section{Reproducibility}
% =====================================================================

One YAML file equals one experiment, and no number in this paper exists without
a committed config behind it. The release carries a number-to-config map guarded
against drift, in the sense that a repository test fails continuous integration
should the map and the config directory ever disagree in either direction.
Alongside it go pinned dependencies and a Python version, per-dataset checksum
manifests recording licences, five datasheets, a model card, and every table
here as a regeneration target. We verified byte-level cross-machine
reproducibility for the tree baselines, the leave-one-country-out fold and the
frozen-encoder probes. Torch GNN training is deterministic only on the same
machine; the resulting $\pm 0.02$ cross-machine variance is documented, and seed
variance of $\pm 0.05$ swamps it anyway. A dedicated suite enforces the leakage
guarantees and runs as its own continuous-integration step. The full suite comes
to 391 tests.

% =====================================================================
\section{Conclusion}
% =====================================================================

One stack does serve both ledgers, and building it that way is what made the
negative results legible. Under a strict protocol, gradient-boosted trees on
graph features still beat the graph neural networks on the financial anchor, and
calibration turns out to matter more than architecture. Transfer between
procurement markets is real but uneven, and it fails on the largest market of
the harder benchmark, a result a macro average would have buried. Cross-domain
transfer runs in one direction only and pays nothing until labels are already
plentiful. Corrupting training labels lifts test performance while validation
falls away, which tells us the standard selection loop cannot be trusted under
temporal shift.

Not one of those findings would have survived a single-seed,
validation-selected, aggregate-only evaluation. That is what we mean by calling
the protocol a contribution. Alongside it we ship a screening instrument that
ranks groups for a human at a fixed budget, explains each flag in the reviewer's
own red-flag vocabulary, and is built so that it cannot call anyone guilty.

\section*{Acknowledgements}

[Placeholder --- funding, compute, and dataset-provider acknowledgements to be
completed before submission.]

\appendices

\section{Number-to-Config Map}
\label{app:repro}

Table~\ref{tab:repro} ties every headline number in this paper to the one
committed configuration that produces it. Should this map and the configuration
directory ever disagree in either direction, a repository hygiene test fails
continuous integration, so the mapping cannot rot in silence. Run any row as
\texttt{collusiongraph <verb> -c configs/experiment/<config>}. Outputs land in a
gitignored results directory and are never committed.

\begin{table}[h]
\caption{Published number $\rightarrow$ config. Multi-seed rows run seeds 0--4
and are resumable; completed seeds are never retrained.}
\label{tab:repro}
\centering
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{p{0.44\columnwidth}p{0.48\columnwidth}}
\toprule
Config (\texttt{configs/experiment/}) & Produces \\
\midrule
\texttt{baselines\_elliptic\_pp} & B1 $0.0576$ / B2 $0.8076$ / B3 $0.8104$ \\
\texttt{gnn\_elliptic\_pp\_gatv2\_} \texttt{focal\_multiseed} & the headline GNN number, $0.4729 \pm 0.0525$ \\
\texttt{gnn\_elliptic\_pp\_gatv2\_} \texttt{wce\_multiseed} & $-$focal ablation, $0.4388 \pm 0.0505$ \\
\texttt{gnn\_elliptic\_pp\_gatv2\_} \texttt{focal\_unidir} & $-$bidirectional edges, $0.3549$ \\
\texttt{ensemble\_elliptic\_pp\_} \texttt{multiseed} & calibrated $0.4434 \pm 0.0501$ vs.\ rank $0.0511 \pm 0.0019$ \\
\texttt{alert\_queue\_elliptic\_pp\_} \texttt{ensemble} & the financial queue: 254 alerts, P@50 $0.32$ \\
\texttt{label\_noise\_elliptic\_pp} & the noise curve, $0.4827 \rightarrow 0.5978$ \\
\texttt{sensitivity\_elliptic\_pp} & NMS / hit-rule invariance grid \\
\texttt{explainer\_ablation\_} \texttt{elliptic\_pp} & PGExplainer 49/50 vs.\ GNNExplainer 12/50 \\
\texttt{baselines\_mendeley\_eu} & B1 $0.3426$ / B2 $0.3925$ / B3 $0.3775$ / B4 $0.3811$ \\
\texttt{baselines\_mendeley\_} \texttt{screens\_ablation} & B2+screens $0.4558$ (best firm-level) \\
\texttt{gnn\_mendeley\_rgcn\_} \texttt{multiseed} & the honest negative, $0.2808 \pm 0.0087$ \\
\texttt{transfer\_loco\_matrix\_} \texttt{mendeley} & 7-fold LOCO, macro lift $1.17$ \\
\texttt{transfer\_lomo\_matrix\_} \texttt{garcia} & 4-market LOMO, macro lift $1.57$ \\
\texttt{cross\_domain\_probe\_} \texttt{proc2fin} & $0.1502$ against $0.065$ prevalence \\
\texttt{cross\_domain\_probe\_} \texttt{fin2proc} & $0.2841$ against $0.358$ prevalence \\
\texttt{label\_efficiency\_*} & both label-efficiency curves \\
\texttt{injection\_recovery\_ocds\_} \texttt{georgia\_multiseed} & coordinated cluster $0.923 \pm 0.162$ @2000 \\
\texttt{baselines\_amlworld} & the 5M-edge negative, $0.0064$ / $0.0062$ \\
\texttt{practitioner\_study} & rater packets and manifest \\
\bottomrule
\end{tabular}
\end{table}

\section{Protocol Guarantees}

A dedicated leakage test, not a convention, enforces each of the following:
strict-inductive temporal splits, as-of feature computation,
leave-one-country and leave-one-market entity isolation, and the alert-unit
rules. That suite runs standalone and also as its own continuous-integration
step on every push. A change that lets leakage back in therefore breaks the
build instead of quietly improving somebody's number. We verified byte-level
cross-machine reproducibility for the tree baselines, the leave-one-country-out
fold and the frozen-encoder probes. Torch GNN training is deterministic on one
machine only. The $\pm 0.02$ cross-machine variance that follows is documented,
and $\pm 0.05$ seed variance dominates it, which is why every learned number
here appears as a multi-seed mean with its standard deviation.

\balance

\begin{thebibliography}{00}
\bibitem{ma2023} X. Ma \emph{et al.}, ``A comprehensive survey on graph anomaly detection with deep learning,'' \emph{IEEE Trans. Knowl. Data Eng.}, vol.~35, no.~12, pp.~12012--12038, 2023.
\bibitem{gadbench} J. Tang, F. Hua, Z. Gao, P. Zhao, and J. Li, ``GADBench: Revisiting and benchmarking supervised graph anomaly detection,'' in \emph{Adv. Neural Inf. Process. Syst.}, vol.~36, 2023, pp.~29628--29653.
\bibitem{motie2024} S. Motie and B. Raahemi, ``Financial fraud detection using graph neural networks: A systematic review,'' \emph{Expert Syst. Appl.}, 2024.
\bibitem{lawal2025} O. Lawal, A. Okolie, and C. Obunadike, ``An explainable graph neural network framework for anti-money laundering in cryptocurrency transactions using the Elliptic dataset,'' \emph{Int. J. Netw. Secur. Appl.}, vol.~17, no.~5/6, 2025.
\bibitem{linemv2025} C.-H. Poon, J. Kwok, C. Chow, and J.-H. Choi, ``LineMVGNN: Anti-money laundering with line-graph-assisted multi-view graph neural networks,'' \emph{AI}, vol.~6, no.~4, art.~69, 2025, doi:10.3390/ai6040069.
\bibitem{lin2026} Y. Lin \emph{et al.}, ``Detecting illicit transactions in Bitcoin: A wavelet-temporal graph transformer approach for anti-money laundering,'' \emph{Sci. Rep.}, vol.~16, art.~1548, 2026, doi:10.1038/s41598-025-23901-3.
\bibitem{multignn} E. Altman \emph{et al.}, ``Realistic synthetic financial transactions for anti-money laundering models,'' in \emph{NeurIPS}, 2023, arXiv:2306.16424.
\bibitem{elliptic} Y. Elmougy and L. Liu, ``Elliptic++: A new financial dataset for graph anomaly detection,'' arXiv:2306.06108, 2023.
\bibitem{weber2019} M. Weber \emph{et al.}, ``Anti-money laundering in Bitcoin: Experimenting with graph convolutional networks for financial forensics,'' in \emph{KDD Workshop on Anomaly Detection in Finance}, 2019.
\bibitem{garcia2022} M. J. Garc\'ia Rodr\'iguez, V. Rodr\'iguez-Montequ\'in, P. Ballesteros-P\'erez, P. E. D. Love, and R. Signor, ``Collusion detection in public procurement auctions with machine learning algorithms,'' \emph{Autom. Constr.}, vol.~133, art.~104047, 2022.
\bibitem{mendeley2025} ``Public procurement cartels: A large-sample testing of screens using machine learning,'' \emph{Int. J. Ind. Organ.}, 2025. Dataset: Mendeley Data f3y4nrn3s6/2.
\bibitem{imhof2025} D. Imhof, E. W. Viklund, and M. Huber, ``Catching bid-rigging cartels with graph attention neural networks,'' arXiv:2507.12369, 2025.
\bibitem{gomes2024} L. Gomes, J. Kueck, M. Mattes, M. Spindler, and A. Zaytsev, ``Collusion detection with graph neural networks,'' arXiv:2410.07091, 2024.
\bibitem{epjds2025} ``Detection of fraud in public procurement using data-driven methods: A systematic mapping study,'' \emph{EPJ Data Sci.}, 2025.
\bibitem{pgexplainer} D. Luo \emph{et al.}, ``Parameterized explainer for graph neural network,'' in \emph{NeurIPS}, 2020.
\bibitem{gnnexplainer} R. Ying \emph{et al.}, ``GNNExplainer: Generating explanations for graph neural networks,'' in \emph{NeurIPS}, 2019.
\bibitem{leiden} V. A. Traag, L. Waltman, and N. J. van Eck, ``From Louvain to Leiden: Guaranteeing well-connected communities,'' \emph{Sci. Rep.}, vol.~9, art.~5233, 2019.
\bibitem{gatv2} S. Brody, U. Alon, and E. Yahav, ``How attentive are graph attention networks?'' in \emph{ICLR}, 2022.
\bibitem{sage} W. Hamilton, Z. Ying, and J. Leskovec, ``Inductive representation learning on large graphs,'' in \emph{NeurIPS}, 2017.
\bibitem{rgcn} M. Schlichtkrull \emph{et al.}, ``Modeling relational data with graph convolutional networks,'' in \emph{ESWC}, 2018.
\bibitem{focal} T.-Y. Lin \emph{et al.}, ``Focal loss for dense object detection,'' in \emph{ICCV}, 2017.
\bibitem{fprate} Reported industry false-positive rates for rule-based AML transaction monitoring (90--95\%) are widely cited from practitioner surveys; comparable academic figures appear in the fraud-detection literature, e.g.\ arXiv:2112.07508 (AAAI 2022) citing Lannoo and Parlour (2021).
\bibitem{saito2015} T. Saito and M. Rehmsmeier, ``The precision-recall plot is more informative than the ROC plot when evaluating binary classifiers on imbalanced datasets,'' \emph{PLoS ONE}, vol.~10, no.~3, 2015.
\bibitem{dominant} K. Ding \emph{et al.}, ``Deep anomaly detection on attributed networks,'' in \emph{SDM}, 2019.
\bibitem{oecd2025} OECD, \emph{Guidelines for Fighting Bid Rigging in Public Procurement (2025 Update)}, OECD Publishing, Paris, 2025, DOI 10.1787/cbe05a56-en.
\bibitem{aiact} European Union, ``Regulation (EU) 2024/1689 laying down harmonised rules on artificial intelligence (AI Act),'' \emph{Official Journal of the European Union}, 2024. See Annex~III, point~5(b).
\bibitem{omnibus} European Union, ``Regulation (EU) 2026/1744 (Digital Omnibus on AI) amending Regulation (EU) 2024/1689,'' \emph{Official Journal of the European Union}, published 24 Jul. 2026, in force 27 Jul. 2026. Defers stand-alone Annex~III high-risk obligations to 2 Dec. 2027.
\bibitem{eccguidance} European Commission, ``Draft guidelines on the classification of high-risk AI systems under the AI Act,'' 2026. The Annex~III~5(b) fraud-detection exception is to be read narrowly and does not extend to AML/CFT checks.
\bibitem{cbuae2025} Central Bank of the UAE, ``Federal Decree-Law No.~6 of 2025,'' Article~149, effective 16 Sep. 2025.
\bibitem{ocds} Open Contracting Partnership, ``Open Contracting Data Standard,'' \url{https://standard.open-contracting.org}.
\end{thebibliography}

\end{document}
