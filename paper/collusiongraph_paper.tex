%============================================================================
%  CollusionGraph — research paper
%  Compile on Overleaf (pdfLaTeX). Figures (fig_*.png) live in the repo root;
%  \graphicspath below finds them locally (../) and when uploaded flat (./).
%  Draft: front matter + method + the joint cross-crime experiment DESIGN.
%  The joint-experiment RESULTS subsection is a marked placeholder until the
%  experiment is run (see Sec. VII-E).
%============================================================================
\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{graphicx}
\graphicspath{{../}{./}}
\usepackage{booktabs}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\usepackage{url}
\usepackage[hidelinks]{hyperref}
\usepackage{balance}
% Figures are supplied as PNGs in the project root (fig_*.png) — upload them to
% Overleaf alongside this file. graphicspath finds them locally and when flat.

\newcommand{\RQ}[1]{\textbf{RQ#1}}
\newcommand{\todo}[1]{\textcolor{red}{\textbf{[#1]}}}

\begin{document}

\title{One Structure, Two Crimes: A Unified Graph Deep\\Learning Framework for
Money-Laundering and Bid-Rigging Detection\\and the Actors Who Bridge Them}

\author{%
\IEEEauthorblockN{Rachit Garg\IEEEauthorrefmark{1},
Kartik Joshi\IEEEauthorrefmark{2},
Prem Kukreja\IEEEauthorrefmark{2},
Gagandeep Singh\IEEEauthorrefmark{2},
Samuel Alex\IEEEauthorrefmark{2}}
\IEEEauthorblockA{\textit{Department of Computer Science and Engineering}\\
\textit{[University / Institution — please complete]}\\
\IEEEauthorrefmark{1}Faculty mentor \quad \IEEEauthorrefmark{2}Author\\
\{rachit.garg, kartik.joshi, prem.kukreja, gagandeep.singh, samuel.alex\}@example.edu}
}

\maketitle

\begin{abstract}
Money laundering and bid rigging are treated as separate detection problems,
yet both are fundamentally crimes of \emph{coordination}: neither can be
committed alone, and any single participant looks ordinary in isolation. We
argue that this shared property makes them one problem in disguise---the
signal lives not in any record but in the geometry of \emph{who deals with
whom}---and we build a single graph-learning system that screens both domains
from one unified representation. Across six public datasets spanning
transaction ledgers and public-procurement records, we combine supervised
graph neural networks, unsupervised graph anomaly detection, hand-computed
structural features and classical baselines through a calibrated ensemble,
roll node scores up into a ranked, explained queue of suspicious \emph{groups},
and evaluate honestly---including where no answer key exists---by planting
known-shape cartels into a real unlabelled network and measuring recovery. We
report the honest picture: gradient-boosted trees remain competitive where
neighbourhood features are pre-computed, calibration before fusion is worth
roughly a $9\times$ improvement over naive fusion, and structure-only
screening reliably recovers cartels whose members physically transact together
($92\%$ at a $1.2\%$ review budget) while remaining blind to turn-taking
schemes. Building on the observation that laundered money funds procurement
corruption and vice versa, we then formulate a \emph{joint cross-crime} model
over a heterogeneous graph in which the two domains share firm nodes through
the common-control motif, and specify a dual-actor injection experiment that
tests whether jointly modelling both crimes recovers entities active in both
that two siloed detectors miss. Throughout, every output is framed as a
screening signal for a human investigator, never a determination of guilt.
\end{abstract}

\begin{IEEEkeywords}
graph neural networks, anomaly detection, money laundering, bid rigging,
collusion detection, financial crime, explainability, class imbalance
\end{IEEEkeywords}

%============================================================ INTRODUCTION
\section{Introduction}
Financial-crime screening and public-procurement integrity are usually studied
by separate communities, with separate datasets, models and evaluation
conventions. This paper starts from a different premise: that
\emph{money laundering} and \emph{bid rigging} are two surface forms of one
underlying phenomenon---illicit \emph{coordination}---and are therefore better
attacked with one system than two.

The premise rests on two shared properties. First, neither crime is possible
alone. Laundering moves criminal proceeds through a chain of accounts until
their origin is untraceable; a bidding cartel requires at least two firms to
secretly agree who wins a contract. Second, and as a direct consequence, any
single participant is individually unremarkable: remove one account from a
laundering ring, or one firm from a cartel, and no rule is broken. The
criminal act is not located in any record but in the \emph{arrangement between
records}. A model that scores entities one at a time is structurally blind to
it; the signal exists only in the relational structure of \emph{who deals with
whom}. This reframes both problems as \emph{graph} problems.

Two further difficulties make the setting realistic and hard. The positive
class is extremely rare---in our primary financial dataset only $\approx\!2\%$
of records are known-illicit---so a model can be $98\%$ accurate while catching
nothing. And ground truth is scarce and partial: $\approx\!77\%$ of that same
dataset carries no label at all. A screening system for this domain must
therefore (i) rank rather than classify, against an explicit review budget;
(ii) function where labels are incomplete or absent; and (iii) explain itself,
because an unexplained alert against a named entity is neither actionable nor
defensible.

We make the following contributions.
\begin{itemize}
\item \textbf{A unified, domain-agnostic pipeline} (Sec.~\ref{sec:method}) that
converts six public datasets across both crimes into one graph intermediate
representation and scores them with four complementary arms fused by
calibration, producing a ranked, de-duplicated, \emph{explained} queue of
group-level alerts.
\item \textbf{An honest, budget-aware evaluation} (Sec.~\ref{sec:results})
including negative results, multi-seed variance, cross-domain transfer, and an
injection-recovery protocol that measures detection where \emph{no answer key
exists} by planting known-shape cartels into a real $488{,}300$-node contract
network.
\item \textbf{A joint cross-crime formulation} (Sec.~\ref{sec:joint}),
motivated by the observation that laundering proceeds fund procurement fraud
and vice versa: a heterogeneous graph in which the two domains share firm nodes
through the common-control motif, together with a \emph{dual-actor injection
experiment} that tests whether joint modelling surfaces entities active in both
crimes that independent detectors miss.
\item \textbf{Ethics as a design constraint} (Sec.~\ref{sec:ethics}): every
output is a calibrated screening signal for a human, enforced at the model,
interface and assistant level.
\end{itemize}

%============================================================ RELATED WORK
\section{Related Work}
\textbf{Graph learning for financial crime.} The Elliptic Bitcoin
dataset~\cite{weber2019} and its Elliptic++ extension~\cite{elmougy2023}
established transaction-graph anti-money-laundering (AML) as a benchmark, and
recent surveys such as GADBench~\cite{tang2023} report a recurring finding that
we also observe: strong tree ensembles are hard to beat when informative node
features are available. Realistic synthetic AML data such as IBM
AMLworld~\cite{altman2023} provides complete labels and monetary amounts absent
from real ledgers. Our graph encoders build on
GATv2~\cite{brody2022}, GraphSAGE~\cite{hamilton2017} and relational
R-GCN~\cite{schlichtkrull2018}; for the unlabelled regime we use graph
autoencoders---DOMINANT~\cite{ding2019} and GAE~\cite{kipf2016}---via
PyGOD~\cite{liu2022pygod}.

\textbf{Bid-rigging and procurement analytics.} Screening for collusion in
public tenders draws on the OECD red-flag guidelines~\cite{oecd2012} and a
growing machine-learning literature~\cite{garcia2022}. Most work is
firm-feature-based; our contribution is to treat the co-bidding record---who
bids against whom, including \emph{losers}---as a graph, which is what makes the
coordination visible.

\textbf{Imbalance, calibration and community structure.} We handle the rare
positive class with focal loss~\cite{lin2017}, restore comparability across
heterogeneous model scores with isotonic calibration~\cite{zadrozny2002}
before fusion, and form group-level alerts with the Leiden community
algorithm~\cite{traag2019}. Explanations use PGExplainer~\cite{luo2020},
which we adopt over GNNExplainer~\cite{ying2019} on the grounds of measured
fidelity.

\textbf{The gap.} To our knowledge no prior system treats laundering and
bid rigging within a \emph{single} graph model, measures transfer between them,
or targets entities that span both crimes. That gap motivates this work.

%============================================================ PROBLEM
\section{Problem Formulation and Research Questions}
\label{sec:problem}
We represent every dataset as a directed, attributed, temporal graph
$G=(V,E,X,T,Y)$ in one common schema: nodes $V$ carry a type
(\texttt{account}, \texttt{address}, \texttt{firm}, \texttt{tender},
\texttt{buyer}, \dots) and first-seen time; edges $E$ carry a type
(\texttt{pays}, \texttt{bids\_on}, \texttt{awarded}, \texttt{buys\_from}), a
timestamp and, where the source records it, an amount; $Y$ assigns a subset of
nodes a label in $\{\text{illicit},\text{licit},\text{unknown}\}$. After this
mapping the downstream system is domain-agnostic---a bank account and a
construction firm are the same kind of object---which is precisely what permits
a single model and a meaningful notion of transfer between domains.

The task is \textbf{budgeted screening}: given a review budget $k$, return a
ranked list of the $k$ groups most worth a human investigator's attention, with
evidence. We evaluate primarily with Precision@$k$ and AUC-PR read against the
prevalence baseline, never accuracy (Sec.~\ref{sec:results}).

We ask four research questions.
\begin{itemize}
\item[\RQ{1}] \emph{Shared structure.} Does coordination leave a
domain-invariant structural signature that one graph model can detect in both
money laundering and bid rigging?
\item[\RQ{2}] \emph{No answer key.} Can detection be measured, and does deep
learning help, where no labels exist---by planting known-shape cartels and
counting recovery?
\item[\RQ{3}] \emph{Transfer.} Does a model trained on one domain (or market)
carry useful signal to another it has never seen?
\item[\RQ{4}] \emph{Cross-crime actors.} Does \emph{jointly} modelling both
crimes over a shared-entity graph improve detection of entities active in
\emph{both}, relative to two independent detectors?
\end{itemize}
\RQ{1}--\RQ{3} are answered in Sec.~\ref{sec:results}. \RQ{4} is the forward
contribution: its method is given in Sec.~\ref{sec:joint} and its experiment is
specified but not yet run (Sec.~\ref{sec:results}-E).

%============================================================ METHOD
\section{The Unified Detection Pipeline}
\label{sec:method}
Fig.~\ref{fig:pipeline} shows the system. An offline pipeline trains models
and writes result files; an always-on, read-only service exposes them. The two
never share a process---the training side cannot be reached from the public
service---so no request path can retrain a model or alter a score. This
\emph{trust boundary} is simultaneously a security property and the reason the
serving side needs no accelerator and runs on commodity hardware.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig_pipeline.png}
\caption{The unified pipeline. Six datasets enter one graph schema; four
scoring arms run in parallel and are combined by a calibrated ensemble; scores
are rolled up into a ranked, de-duplicated, explained queue. Everything below
the trust boundary is read-only.}
\label{fig:pipeline}
\end{figure}

\subsection{Unified graph schema}
Per-dataset adapters map raw public files into the schema of
Sec.~\ref{sec:problem}, with checksum verification so a corrupted or swapped
input halts ingestion rather than silently training on the wrong data. Absent
fields (e.g.\ monetary amounts on ledgers that omit them) remain absent; no
value is imputed.

\subsection{Four scoring arms}
On the same graph we run, in parallel:
\emph{(i) Supervised GNNs}---GATv2, GraphSAGE and R-GCN, trained with focal
loss to counter the $\approx\!2\%$ positive rate;
\emph{(ii) Unsupervised graph anomaly detection}---DOMINANT and GAE
autoencoders, which require no labels and carry the strongest
deployment-realistic result (Sec.~\ref{sec:results});
\emph{(iii) Structural features}---node degrees, motif counts, $k$-core, and
procurement bid-screens fed to a gradient-boosted model; and
\emph{(iv) Classical baselines}---XGBoost/LightGBM as an honesty yardstick so
no deep-learning claim is made against a straw man.

\subsection{Calibrated fusion}
Heterogeneous model scores are not directly comparable: two models may rank
equally well yet occupy different score ranges, and naively averaging lets a
loud weak model dominate a quiet strong one. Each arm's scores are therefore
mapped to calibrated probabilities via isotonic regression on held-out
labelled nodes \emph{before} fusion. The ordering matters more than any single
model (Sec.~\ref{sec:results}).

\subsection{Group formation and ranking}
Node scores are useless to a human at ledger scale. We detect communities with
Leiden on the test-window graph, aggregate member scores to a community score,
suppress overlapping communities (NMS-style, Jaccard $\geq 0.5$), cap group
size, and cut at the review budget. Crucially this stage uses \emph{no labels},
which is what allows the identical procedure to run on datasets that have none.

\subsection{Explanation}
For each top alert, PGExplainer isolates the few connections the decision
depended on (verified for both sufficiency and necessity); a deterministic
motif matcher names the pattern against nine curated collusion shapes drawn
from FATF and OECD indicator tables; and the corresponding official red-flag
indicator is cited. The matcher runs on \emph{every} alert (it needs only
graph rules, not the learned explainer), so a named pattern or an honest
``none found'' is always available, never ``not checked''.

%============================================================ JOINT MODEL
\section{Joint Cross-Crime Modelling}
\label{sec:joint}
The suggestion that motivates \RQ{4} is criminologically documented: the
proceeds of procurement corruption are laundered, and laundered funds finance
cartels. If true, the same real-world actors span both crimes---and a system
that silos the two will miss exactly the entities that hide in the gap between
them.

We must separate two claims. The weaker, which our transfer results already
support (Sec.~\ref{sec:results}), is that coordination is \emph{structurally
similar} across domains. The stronger, which is the target of \RQ{4}, is that
the \emph{same entities} appear in both. Demonstrating the stronger claim
requires a substrate in which entities are linked across domains---something no
single one of our six datasets provides, since they are siloed by domain with
no shared identifier space. We address this in two ways.

\subsection{A heterogeneous joint graph}
Our schema already admits a single graph that mixes \texttt{pays} edges with
\texttt{bids\_on}/\texttt{awarded} edges over shared \texttt{firm} nodes
(Fig.~\ref{fig:joint}); no new infrastructure is required. The natural seam
between the domains is the \emph{common-control} motif---hidden or shared
ownership---which is the one pattern that already appears in both the FATF and
the OECD indicator sets. A message-passing model over this graph can, in
principle, raise the prior on a firm's procurement behaviour from its financial
behaviour and vice versa.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig_joint_graph.png}
\caption{The joint cross-crime graph. A financial layer (money moving through a
ring of accounts) and a procurement layer (firms bidding on tenders) share a
\emph{dual actor} via the common-control motif. A joint model message-passes
across this seam, raising the prior on a firm in one layer from its behaviour
in the other; two siloed detectors cannot.}
\label{fig:joint}
\end{figure}

\subsection{The dual-actor injection experiment}
\label{sec:dualactor}
Because no public ground truth of ``entity active in both crimes'' exists, we
extend our injection-recovery methodology (Sec.~\ref{sec:results}) from single-
to cross-domain. Into a joint graph we plant \emph{dual actors}: a synthetic
entity (or shared owner) that simultaneously participates in a laundering motif
in the financial layer and a cartel motif in the procurement layer, linked by
common control. We then compare three detectors at a fixed review budget:
\begin{enumerate}
\item[(B1)] the financial detector alone (procurement layer hidden);
\item[(B2)] the procurement detector alone (financial layer hidden);
\item[(J)] the joint model over the full heterogeneous graph.
\end{enumerate}
The primary outcome is \emph{dual-actor recall}: the fraction of planted dual
actors surfaced within budget. The hypothesis is $J > \max(\text{B1},
\text{B2})$---i.e.\ that cross-domain message passing recovers actors that
either siloed view alone ranks too low to review. Secondary outcomes are
per-layer recall (does the joint view cost anything on single-domain actors?)
and the review-budget curve. A real-data leg using corporate beneficial-
ownership linkage (OCDS firms $\rightarrow$ ownership registries) is left to
future work; the synthetic experiment is the controlled test of the mechanism.

%============================================================ DATASETS
\section{Datasets}
\label{sec:data}
We use six public datasets across both domains (Table~\ref{tab:data}); all
counts are measured from our own ingested copy, not copied from the source
papers. Raw data is never redistributed---each ships with a download script and
a per-file checksum.

\begin{table}[t]
\caption{Datasets. ``Labels'' is the known-illicit share; ``---'' means no
answer key.}
\label{tab:data}
\centering
\small
\begin{tabular}{@{}llrrl@{}}
\toprule
\textbf{Dataset} & \textbf{Domain} & \textbf{Nodes} & \textbf{Edges} & \textbf{Labels}\\
\midrule
Elliptic        & money   & 203{,}769 & 234{,}355   & $2.2\%$ illicit\\
Elliptic++      & money   & 822{,}942 & 2{,}868{,}964 & partial\\
AMLworld (HI-S) & money   & 515{,}088 & 5{,}078{,}345 & full ($1.2\%$)\\
Mendeley EU     & tenders & 14{,}555  & 24{,}251    & 73 cartel cases\\
Garc\'ia R.\    & auctions& 77{,}007  & 111{,}046   & confirmed cartels\\
OCDS Georgia    & tenders & 488{,}300 & 1{,}449{,}077 & --- (unlabelled)\\
\bottomrule
\end{tabular}
\end{table}

Elliptic spans 49 time steps with a documented distribution shift at step~43
(a marketplace shutdown) that we report per-step rather than average over.
Mendeley is a \emph{case-control study sample}---$\approx\!42\%$ of rows are
cartel by construction---so its scores may only be read against its own
prevalence and never as a real-world hit rate. OCDS Georgia, at roughly $50\times$
Mendeley's size and with no labels, is our injection-recovery substrate and
records $687{,}336$ losing bids, the co-bid evidence cartel detection needs.

%============================================================ RESULTS
\section{Experimental Results}
\label{sec:results}
All GNN results are reported over five random seeds as mean\,$\pm$\,std;
comparisons use paired-bootstrap significance; every score is read against the
prevalence (random-guess) baseline. Models are trained on the past and tested
on the future; no hyperparameter search was run on either side, and we state so
because it cuts against inflating our own numbers.

\subsection{Where deep learning wins---and where it does not}
On Elliptic, the strongest classical baseline outperforms our GNNs
(Table~\ref{tab:elliptic}). We report this openly: Elliptic ships hand-built
neighbourhood aggregates as node features, which the tree consumes directly, so
the graph gives it little extra. This replicates the central GADBench
finding~\cite{tang2023} and is the honest baseline against which the rest of the
paper should be read.

\begin{table}[t]
\caption{Elliptic (money). Prevalence baseline AUC-PR $=0.065$.}
\label{tab:elliptic}
\centering\small
\begin{tabular}{@{}lrr@{}}
\toprule
\textbf{Model} & \textbf{AUC-PR} & \textbf{P@100}\\
\midrule
Hand-written rules            & 0.058 & 0.00\\
XGBoost                       & 0.808 & 1.00\\
\textbf{XGBoost + graph feats}& \textbf{0.810} & 1.00\\
GATv2 (focal, 5 seeds)        & $0.473\pm0.053$ & $0.81\pm0.24$\\
GATv2 (weighted-CE, 5 seeds)  & $0.439\pm0.051$ & $0.72\pm0.15$\\
Autoencoders / floor          & 0.039--0.055 & ---\\
\bottomrule
\end{tabular}
\end{table}

\subsection{Calibration before fusion (\RQ{1})}
Fusing raw model scores collapses the ensemble to the guessing level
($0.051\pm0.002$); calibrating each arm first lifts it to $0.443\pm0.050$---an
$\approx\!9\times$ difference on the same models and data. The ordering, not
the models, is the finding.

\subsection{Detection without an answer key (\RQ{2})}
Into the unlabelled $488{,}300$-node OCDS Georgia network we plant $100$ cartels
($940$ member firms) of five known shapes and measure recovery over five seeds,
reviewing only the top $2{,}000$ ($\approx\!1.2\%$) groups
(Fig.~\ref{fig:injection}). Cartels whose firms \emph{physically bid together}
are recovered at $92\%$; a hidden-common-owner shape at $58\%$; and turn-taking,
market-carve-up and cover-bidding schemes escape structure-only screening
almost entirely. Here the classical baseline cannot compete---there is nothing
to train on---while the unsupervised deep model recovers the bid-together clique
at $0.91$ versus $0.12$ for a non-learning floor. This is the clearest case of
deep learning doing something no alternative in our stack can.

\begin{figure}[t]
\centering
\includegraphics[width=0.94\columnwidth]{fig_injection_recovery.png}
\caption{Injection recovery on the unlabelled Georgia network (best arm, top
$2{,}000$, 5 seeds; error bars are $\pm$ std). Structure-only screening catches
bid-together coordination and is characterisably blind to turn-taking schemes.}
\label{fig:injection}
\end{figure}

\subsection{Cross-domain transfer (\RQ{3})}
Holding out one market/country entirely and testing on it: on Garc\'ia auctions
the model transfers positively to \emph{every} held-out market (macro lift
$1.57\times$ over guessing); on Mendeley it is market-dependent (macro lift
$1.17\times$) and its largest country underperforms guessing ($0.90\times$)---%
published per-fold rather than as a flattering average. Between crimes, a
procurement-trained model carries weak positive signal to the financial task
($0.150$ vs.\ prevalence $0.065$), while the reverse direction is negative.
Coordination structure transfers, but conditionally.

\subsection{The dual-actor experiment (\RQ{4})}
\label{sec:joint-results}
\todo{Pending. The dual-actor injection experiment of Sec.~\ref{sec:dualactor}
is specified but not yet run; this subsection will report dual-actor recall for
the two siloed detectors (B1, B2) and the joint model (J), the per-layer cost
of the joint view, and the review-budget curve. The claim under test is
$J>\max(\text{B1},\text{B2})$. A negative outcome---that joint modelling does
not help---is itself a reportable finding and will be presented as such.}

%============================================================ ETHICS
\section{Ethics and Responsible Use}
\label{sec:ethics}
Every output of this system is a calibrated \emph{screening} signal that
prioritises human review; none is, or may be presented as, a determination of
wrongdoing. This is enforced, not merely stated: the interface carries the
caveat on every screen and every API response; the GenAI assistant is
read-only, must ground every number in a tool lookup, and automatically
rewrites accusatory language, passing a fixed golden-test gate before release.
The cross-crime direction (\RQ{4}) \emph{raises} the stakes---linking an entity
across two crime datasets is a stronger imputation than flagging it in one---so
the screening-only framing and human-in-the-loop requirement are not optional
add-ons but preconditions for that line of work. Unconfirmed is never treated
as either innocent or guilty.

%============================================================ LIMITATIONS
\section{Limitations}
\label{sec:limits}
Our strongest headline result (injection recovery) is synthetic by necessity,
because real answer keys do not exist at scale; we are explicit about this. The
joint cross-crime claim currently rests on a controlled synthetic experiment
(Sec.~\ref{sec:dualactor}); a real-data leg requires cross-domain entity
linkage (e.g.\ beneficial-ownership registries) that we have not yet assembled.
Structure-only screening is characterisably blind to turn-taking cartels that
leave no co-occurrence trace; catching those needs price and timing signals.
Finally, the case-control construction of some procurement data forbids reading
its precision as a real-world rate.

%============================================================ CONCLUSION
\section{Conclusion and Future Work}
We have argued that money laundering and bid rigging are one problem---illicit
coordination---and built a single graph-learning system that screens both from
a unified representation, evaluates honestly including where no labels exist,
and explains every alert. The results are reported without varnish: classical
trees remain competitive where features are pre-computed, calibration before
fusion is the highest-value design choice, and structure-only screening has a
precise, stated blind spot. Building on the documented overlap between the two
crimes, we formulated a joint cross-crime model over a heterogeneous
shared-entity graph and specified a dual-actor injection experiment to test
whether joint modelling recovers entities that siloed detectors miss. Running
that experiment---and adding a real beneficial-ownership linkage leg---is the
immediate next step, and the substance of the paper's forward contribution.

%============================================================ BIB
\balance
\begin{thebibliography}{99}
\bibitem{weber2019} M.~Weber \emph{et al.}, ``Anti-money laundering in Bitcoin:
Experimenting with graph convolutional networks for financial forensics,''
\emph{arXiv:1908.02591}, 2019.
\bibitem{elmougy2023} Y.~Elmougy and L.~Liu, ``Demystifying fraudulent
transactions and illicit nodes in the Bitcoin network,'' in \emph{Proc. ACM
SIGKDD}, 2023.
\bibitem{altman2023} E.~Altman \emph{et al.}, ``Realistic synthetic financial
transactions for anti-money laundering models,'' in \emph{Proc. NeurIPS Datasets
and Benchmarks}, 2023.
\bibitem{tang2023} J.~Tang \emph{et al.}, ``GADBench: Revisiting and
benchmarking supervised graph anomaly detection,'' in \emph{Proc. NeurIPS},
2023.
\bibitem{brody2022} S.~Brody, U.~Alon, and E.~Yahav, ``How attentive are graph
attention networks?'' in \emph{Proc. ICLR}, 2022.
\bibitem{hamilton2017} W.~Hamilton, Z.~Ying, and J.~Leskovec, ``Inductive
representation learning on large graphs,'' in \emph{Proc. NeurIPS}, 2017.
\bibitem{schlichtkrull2018} M.~Schlichtkrull \emph{et al.}, ``Modeling
relational data with graph convolutional networks,'' in \emph{Proc. ESWC}, 2018.
\bibitem{ding2019} K.~Ding \emph{et al.}, ``Deep anomaly detection on attributed
networks,'' in \emph{Proc. SIAM SDM}, 2019.
\bibitem{kipf2016} T.~N.~Kipf and M.~Welling, ``Variational graph
auto-encoders,'' \emph{arXiv:1611.07308}, 2016.
\bibitem{liu2022pygod} K.~Liu \emph{et al.}, ``PyGOD: A Python library for graph
outlier detection,'' \emph{arXiv:2204.12095}, 2022.
\bibitem{lin2017} T.-Y.~Lin \emph{et al.}, ``Focal loss for dense object
detection,'' in \emph{Proc. ICCV}, 2017.
\bibitem{zadrozny2002} B.~Zadrozny and C.~Elkan, ``Transforming classifier
scores into accurate multiclass probability estimates,'' in \emph{Proc. ACM
SIGKDD}, 2002.
\bibitem{traag2019} V.~A.~Traag, L.~Waltman, and N.~J.~van Eck, ``From Louvain
to Leiden: guaranteeing well-connected communities,'' \emph{Scientific Reports},
vol.~9, 2019.
\bibitem{chen2016} T.~Chen and C.~Guestrin, ``XGBoost: A scalable tree boosting
system,'' in \emph{Proc. ACM SIGKDD}, 2016.
\bibitem{ying2019} R.~Ying \emph{et al.}, ``GNNExplainer: Generating
explanations for graph neural networks,'' in \emph{Proc. NeurIPS}, 2019.
\bibitem{luo2020} D.~Luo \emph{et al.}, ``Parameterized explainer for graph
neural network,'' in \emph{Proc. NeurIPS}, 2020.
\bibitem{garcia2022} M.~Garc\'ia Rodr\'iguez \emph{et al.}, ``Bid rigging
detection using machine learning,'' \emph{[journal/venue, year --- please
verify]}.
\bibitem{oecd2012} OECD, ``Guidelines for fighting bid rigging in public
procurement,'' 2012.
\bibitem{fatf2012} Financial Action Task Force, ``International standards on
combating money laundering,'' FATF Recommendations, 2012.
\bibitem{ocds} Open Contracting Partnership, ``Open Contracting Data Standard
(OCDS),'' \url{https://standard.open-contracting.org}.
\end{thebibliography}

\end{document}
