# Ensemble-scored alert queue (Week-6 ledger item): Leiden on the Elliptic++
# test window, members scored by the CALIBRATED-FUSION ensemble (already
# probabilities — no re-calibration).
dataset: elliptic_pp
domain: financial
store_root: data/interim
scores_dir: eval_outputs/elliptic_pp/ensemble
scores_file: scores_ensemble_calibrated.parquet
precalibrated: true
output_dir: eval_outputs/elliptic_pp/alert_queue_ensemble
model_run_id: ens_cal_s0
seed: 0

split:
  test_start: 35

resolution: 1.0
min_community_size: 2
top_p: 0.25
budgets: [50, 100, 200]
