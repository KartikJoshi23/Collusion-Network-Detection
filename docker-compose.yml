# Multi-service blueprint (docs/deployment.md §2): `docker compose up` runs the
# product from precomputed artifacts. Batch ML is NOT a service — run the
# pipeline first (poe demo-artifacts / collusiongraph CLI), then serve outputs.
services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
    volumes:
      - ./eval_outputs:/artifacts/eval_outputs:ro
      - ./data/interim:/artifacts/data/interim:ro
    environment:
      # Copilot LLM key (§4.6) — set in the shell or a compose .env file;
      # empty means /copilot/health reports configured=false, serving still up
      - NVIDIA_API_KEY=${NVIDIA_API_KEY:-}
    expose:
      - "8000"

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "8080:80" # open http://localhost:8080 for the demo
    depends_on:
      - api

  # copilot: joins in Week 11 (§4.6) — LLM key via env (NVIDIA NIM preferred).
